function returnArr(index_parameter){
  const arr = [2, 4, 5, 8];
  for(let i = 0; i < arr.length; i++){
    result = arr[index_parameter];
  }

  return 'The value is ' + result;
}

console.log(returnArr(0));

// index is the location of an element inside of the array
// parameter is like a variable of a function